
@using WebApplication1.Controllers
@using WebApplication1.Models

@model IQueryable<Tracks>


@{
    ViewData["Title"] = "Home Page";
}


<div id="Player">@{/*
    <audio id="audioPlayer" controls loop preload="metadata">
        <!--<source src="/audio/Нашид - Машаллах.mp3" type="audio/mpeg">
        Your browser don`t support audio player... Update browser and try again ;) -->
    </audio>*/}
</div>

@{/*
<div class="Track_Block">
    <h3>Недавно прослушено</h3>
</div>
*/}

@if(Helper.user?.UserAccessLevel == "admin")
{
    <div class="btn btn-success btn-add-track">
        <a class="text-white"  asp-area="" asp-controller="Tracks" asp-action="Index">Add Track</a>
    </div>
}


<div class="search-menu">
    <div>
        <form  method="post">
            <select name="searchId">
                <option value="1">Track name</option>
                <option value="2">Artist</option>
                <option value="3">Most popular</option>
            </select>
            <input asp-area="" asp-controller="Home" asp-action="OrderBy" type="submit" class="btn btn-primary" value="order by"/>
       </form>
    </div>
    <div>
        <form  method="post">
            <input name="searchString" id="elastic" type="text" placeholder="search"/>
            <input asp-area="" asp-controller="Home" asp-action="Search" type="submit" hidden/>
        </form>
    </div>
</div>

<!-- /*************** bootstrap from https://codepen.io/MiRaxX69/pen/PvaEaG *****************/ -->
<!-- jade: compressed-->

@if(@Model != null)
{
 <ul class="audio-players">
    @foreach (var @track in @Model)
    {
     <li class="elastic">
        <div onclick="playAudio('@{@track.Audio}')" class="audio-player js-audio-player">
            <button class="audio-player__control">
                <div class="audio-player__control-icon"></div>
            </button>
            <div class="audio-player__title">
                <h5>@track.Artist</h5>
                <p>@track.TrackName</p>
            </div>
            <img src="@track.Picture"/>
        </div>
    </li>   
    }
</ul>
   
}
<!-- /********************************************************************/ -->


